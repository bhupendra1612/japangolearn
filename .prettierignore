node_modules
.next
.turbo
.expo
dist
out
coverage
web-build
pnpm-lock.yaml
supabase/migrations/*.sql

# Generated by `wrangler types`; regenerating it would re-break a formatting gate.
apps/api/cloudflare-env.d.ts

# Pasted by hand into the Supabase Auth dashboard, never compiled. Prettier
# wraps the text inside the {{ .Token }} div, which risks visible whitespace in
# a code rendered at letter-spacing: 10px, and email clients are fragile about it.
docs/email-templates/
