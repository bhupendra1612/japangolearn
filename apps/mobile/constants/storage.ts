// AsyncStorage keys. Shared so the onboarding screen and the redirect hub in
// app/index.tsx cannot drift apart.
export const ONBOARDED_KEY = "@japangolearn_onboarded";
export const GUEST_KEY = "@japangolearn_guest_mode";
